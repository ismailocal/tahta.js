import { describe, it, expect, vi } from 'vitest';
// import { WhiteboardStore } from './Store'; 

describe('WhiteboardStore', () => {
  it.skip('should notify subscribers on state change', () => {
    // const store = new WhiteboardStore();
    // const handler = vi.fn();
    // store.subscribe(handler);
    // store.addShape(mockShape());
    // expect(handler).toHaveBeenCalledTimes(1);
  });

  it.skip('should not notify if state is identical', () => {
    // const store = new WhiteboardStore();
    // store.setState({ shapes: [] });  // no-op
    // expect(handler).not.toHaveBeenCalled();
  });
});
